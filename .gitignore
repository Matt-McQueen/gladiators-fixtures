# Generated by generate_ics.py, published to gh-pages by CI rather than
# committed here. Note this file is NOT copied into the publish directory
# -- doing that once made the deploy silently drop the feed, because
# peaceiris/actions-gh-pages honours a .gitignore found in publish_dir.
gladiators-fixtures.ics
fixtures_state.json

__pycache__/
